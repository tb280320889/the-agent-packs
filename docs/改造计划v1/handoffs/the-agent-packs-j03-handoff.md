# Handoff: 改造计划 v1 文档包初始化

## 1. 交接对象
- 来源 bead：the-agent-packs-j03
- 下一 bead：待拆分
- 来源里程碑：M0
- 目标角色：项目内部维护 agent / 迭代开发子 agent / 文档型 agent

## 2. 已完成什么
- 新增 `docs/改造计划v1/` 文档目录，并按原正式文档包风格补齐了 `00~05` 共享文档与 `10~51` 的里程碑开发指导、必要上下文文档。
- 明确了角色模型、分层索引模型、package 注册表与命名空间治理方向、文档吸收管道方向，以及后续迁移实施步骤。
- 新增了改造版 Context Snapshot / Handoff 模板，并补了一份初始化阶段快照。

## 3. 下一位 agent 可直接依赖什么
- 改造计划总索引：`docs/改造计划v1/00-总索引与使用说明.md`
- 目标系统模型：`docs/改造计划v1/02-目标系统模型_总编排_子编排与分层索引.md`
- 角色模型：`docs/改造计划v1/03-角色体系与协作模型.md`
- package 命名与注册表规则：`docs/改造计划v1/31-M2_上下文_命名规则_注册表字段与冲突裁决.md`

## 3.1 阅读边界提醒
- 不要因为 `docs/改造计划v1/` 已存在就全量阅读整个目录。
- 新 agent 只应根据 bead 与开发需求，进入当前相关里程碑的最小阅读集。
- 若 bead 只涉及 route 分层设计，则优先读 `20 + 21 + 22`；若只涉及 package 命名治理，则优先读 `30 + 31`。

## 4. 下一位 agent 必须先做什么
- 先 claim：当前要执行的 bead；一个开发 agent 只主负责当前 bead，不预先规划全量后续 bead。
- 先阅读：`docs/改造计划v1/00-总索引与使用说明.md`，然后只读当前 bead 所属里程碑的最小阅读集。
- 先验证：`go test ./...`。

## 5. 不要做什么
- 不要直接把这套改造计划当作现有正式主线的替代版。
- 不要在没有 bead 和迁移方案的情况下直接改 `internal/query/query.go` 或 Blueprint 节点语义。
- 不要继续新增裸名 package。

## 6. 风险与未决项
- 仍需决定是否把 `docs/改造计划v1/` 显式挂入现有正式总索引或 README。
- 仍需为 M1、M2 的具体实现草案分别创建 bead。

## 7. 推荐下一动作
- 新 session 中，先根据真实开发需求创建或 claim 一个 bead，再让该 agent 只按相关里程碑最小阅读集进入。
- 若当前需求是“分层索引 / 总编排”，优先进入 M1；若当前需求是“workflow package 命名冲突治理”，优先进入 M2。
