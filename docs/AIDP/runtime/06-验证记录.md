# 验证记录

## V-01 AIDP 文档集生成与入口切换
- 日期：2026-03-16
- 范围：`docs/AIDP/` 首版生成，`AGENTS.md` 切换入口
- 验证方式：文档结构检查、入口路径检查
- 结果：pending
- 备注：如需仓库级验证，可补充执行 `go test ./...`

## V-02 system AIDP v2.0.1 思想与 local AIDP 对齐检查
- 日期：2026-03-16
- 范围：system AIDP 的版本治理、增强迭代机制与 local AIDP 的协同模型
- 验证方式：阅读 `docs/AIDP-zh-v2.0.0/AIDP-zh/README.md`、`VERSION.md`、`COMPATIBILITY.md`、`GOVERNANCE.md`、`protocol/10-增强开发与迭代协议.md`、`adapters/gsd/06-增强开发与变更落地.md`，并对照 local AIDP 当前定位
- 结果：pass
- 备注：确认 system AIDP 与“system AIDP -> local AIDP -> GSD -> 开发 agent”这一思想模型一致，无根本性语义冲突

## V-03 ready-with-guardrails 基线判定对齐
- 日期：2026-03-16
- 范围：M5/M6 长期有效结论迁入 local AIDP 的准确性
- 验证方式：对照 `docs/改造计划v1/62-M5_整改结果与收口结论.md`、`docs/改造计划v1/72-M6_验收结果与V1收口结论.md` 与 local AIDP `core/10`、`core/11`
- 结果：pass
- 备注：已明确当前是“机制 ready-with-guardrails”，而非“真实第二主域样板已落地”

## V-04 身份视角与消费契约语义整改检查
- 日期：2026-03-16
- 范围：README、MANIFEST、`core/00`、`core/01`、`core/03`、`core/06`、`core/10`、协议与 runtime 文档
- 验证方式：检查是否已明确项目为增强包生产者系统，是否已区分生产者 / 消费侧 agent / 开发 agent 三类身份，是否已将渐进披露升级为正式上下文交付契约
- 结果：pass
- 备注：已完成首轮语义校准；后续仍需在实现与测试层继续验证“最小但完整”在具体主域上的成立

## V-05 WXT 消费契约样例验证口径检查
- 日期：2026-03-16
- 范围：`core/09`、`core/10`、`core/11`、`adapters/gsd/06`、`runtime/10-WXT-消费契约验证样例.md`
- 验证方式：检查是否已为 `WXT -> wxt-manifest` 建立文档级消费契约样例，确认存在应交付内容、不应默认交付内容、最小但完整判断口径、文档级验证步骤与当前限制说明
- 结果：pass
- 备注：第二轮整改已把消费契约继续映射到验证口径；后续需要把该口径进一步映射到实现验证与测试流程

## V-06 Phase 01.1 结构治理与提交门禁验证
- 日期：2026-03-16
- 范围：`.planning/phases/01.1-brownfield-git-commit/*`、`docs/archive/2026-03-phase-01.1/*`、runtime 回写
- 验证方式：执行 `go test ./...`，并检查结构审计/目标树/迁移映射/归档清单是否齐备，确认 `ARCHIVE-LIST` 的删除门禁为 `human-approval`
- 结果：pass
- 备注：本轮未执行删除；checkpoint 采用 `option-b`（暂不删除），符合“先归档后删除”约束

## V-07 Phase 01-03 解析回归与全量测试验证
- 日期：2026-03-16
- 范围：解析回归 fixture 与全量测试闭环
- 验证方式：执行 `go test ./... -run TestM4RegressionParsing` 与 `go test ./...`
- 结果：pass
- 备注：新增解析回归 fixture，确保未知字段与多行 frontmatter 行为稳定
