# wxt-manifest 路由规则

## 命中条件
- task 包含 `manifest` / `permissions` / `host permissions` / `store submission`。
- route 已命中 `L1.wxt.manifest`。

## 必带横线
- `security-permissions`
- `release-store-review`

## 不命中示例
- task 主要关注 content script 注入策略。
- task 主要关注 background runtime 架构。
