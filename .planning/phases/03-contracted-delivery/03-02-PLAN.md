---
phase: 03-contracted-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - internal/model/model.go
  - internal/validator/registry.go
  - internal/validator/contract_delivery.go
  - tests/m3_contract_bundle_test.go
autonomous: true
requirements:
  - CONT-03
must_haves:
  truths:
    - "维护侧可通过可重复命令执行交付契约检查，并得到 pass/fail/warning 结论。"
    - "P0 问题（跨域混入、required 缺失、规则不可追溯）会触发 hard fail。"
    - "检查输出包含失败项、规则映射与最小修复建议，可供后续 phase 接力。"
  artifacts:
    - path: "internal/validator/contract_delivery.go"
      provides: "契约检查器与 P0/warning 分级策略"
      contains: "validateContractDelivery"
    - path: "internal/validator/registry.go"
      provides: "contract-delivery validator 注册"
      contains: "validator-contract-delivery"
    - path: "tests/m3_contract_bundle_test.go"
      provides: "WXT 正例 + 负例（hard fail）可重复检查矩阵"
      contains: "TestContractDeliveryValidatorNegative"
  key_links:
    - from: "internal/validator/registry.go"
      to: "internal/validator/contract_delivery.go"
      via: "validator 名称到实现映射"
      pattern: "validator-contract-delivery"
    - from: "tests/m3_contract_bundle_test.go"
      to: "internal/validator/contract_delivery.go"
      via: "断言错误码/严重级别/修复建议"
      pattern: "severity|code|repair_suggestions"
---

<objective>
建立 Phase 3 的可执行契约检查器，把“最小且完整”从文档约束升级为可重复验证的自动化规则。

Purpose: 闭合 CONT-03，使交付契约可稳定复验，并为 Phase 4 的 validation/runtime 治理提供结构化输入。
Output: contract delivery validator、分级失败语义、正负例测试矩阵。
</objective>

<execution_context>
@C:/Users/唐斌/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/唐斌/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-contracted-delivery/03-CONTEXT.md
@.planning/phases/03-contracted-delivery/03-RESEARCH.md
@.planning/phases/03-contracted-delivery/03-01-PLAN.md
@internal/model/model.go
@internal/validator/registry.go
@internal/validator/runner.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 contract delivery validator 与分级失败策略</name>
  <files>internal/validator/contract_delivery.go, internal/model/model.go</files>
  <action>新增 validator 实现并对齐锁定决策：P0（跨域混入、required 缺失、source_rule 不可追溯）输出 error 并导致 hard fail；非阻断问题（human note 不清晰但结构完整）输出 warning。结果中必须包含 failure 列表、规则映射（rule_ref/source_rule）和最小修复建议，避免仅返回 pass/fail 文本。</action>
  <verify>go test ./... -run "TestContractDeliveryValidatorPositive|TestContractDeliveryValidatorNegative"</verify>
  <done>契约检查输出具备可机检的失败分级、规则映射与修复建议。</done>
</task>

<task type="auto">
  <name>Task 2: 注册 validator 并接入现有 runner 执行链路</name>
  <files>internal/validator/registry.go</files>
  <action>将 `validator-contract-delivery` 注册到 validator registry，确保可被 validation plan 调用。保持现有 runner 调用约定不破坏，必要时补齐缺省字段处理，避免新增检查器导致旧 validator 行为回归。</action>
  <verify>go test ./... -run "TestM3GoldenCompleted|TestContractDeliveryValidatorPositive"</verify>
  <done>contract checker 可通过现有 runner 稳定执行，且不破坏已有 validation 流程。</done>
</task>

<task type="auto">
  <name>Task 3: 扩展正负例测试矩阵覆盖 hard-fail 与 warning 语义</name>
  <files>tests/m3_contract_bundle_test.go</files>
  <action>在同一测试文件新增负例（至少 1 个明确 P0）与 warning 例：负例可通过构造跨域混入或缺失 required/rationale 触发；断言 status、错误码、severity 与修复建议。确保测试可重复触发、可复验，不依赖人工检查。</action>
  <verify>go test ./... -run "TestContractDeliveryValidator.*"</verify>
  <done>存在可重复执行的契约检查矩阵，至少覆盖 1 个 WXT 正例与 1 个负例。</done>
</task>

</tasks>

<verification>
- 运行 `go test ./... -run "TestContractDeliveryValidator.*"`，确认正例通过、负例 hard fail、warning 语义稳定。
- 运行 `go test ./... -run "TestM3GoldenCompleted"`，确认接入新 checker 后现有主流程不回归。
</verification>

<success_criteria>
- 合同检查可通过单条命令重复执行且输出结构化结论。
- P0 问题被 hard fail 阻断，warning 问题可保留为非阻断。
- 输出包含失败项、规则映射和最小修复建议，可直接进入后续治理阶段。
</success_criteria>

<output>
After completion, create `.planning/phases/03-contracted-delivery/03-02-SUMMARY.md`
</output>
