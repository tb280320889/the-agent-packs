---
phase: 03-contracted-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/model/model.go
  - internal/query/query.go
  - tests/m3_contract_bundle_test.go
autonomous: true
requirements:
  - CONT-01
  - CONT-02
must_haves:
  truths:
    - "消费侧拿到的 context bundle 仅包含目标主域必需知识，不混入无关主域节点。"
    - "每个 include 与 exclude 决策都有 machine-readable 字段与 human-readable 说明。"
    - "当最小化与完整性冲突时，结果会显式记录放宽最小化的依据。"
  artifacts:
    - path: "internal/model/model.go"
      provides: "Context Delivery Contract 结构（include/exclude rationale 字段）"
      contains: "type ContextBundle"
    - path: "internal/query/query.go"
      provides: "BuildContextBundle 产出 required 判定与 include/exclude rationale"
      contains: "BuildContextBundle"
    - path: "tests/m3_contract_bundle_test.go"
      provides: "WXT 正例下最小且完整 + rationale 可追溯回归"
      contains: "TestContractBundleWXTPositive"
  key_links:
    - from: "internal/query/query.go"
      to: "internal/model/model.go"
      via: "bundle 组装 ContractDecision 并写入 ContextBundle"
      pattern: "reason_code|source_rule|scope|decision_basis"
    - from: "tests/m3_contract_bundle_test.go"
      to: "internal/query/query.go"
      via: "正例断言 include/exclude 双向理由和域边界"
      pattern: "BuildContextBundle|Contract"
---

<objective>
把 Context Bundle 从“仅结构化内容”升级为“结构化内容 + 可解释交付契约”，确保交付对象既最小又完整并可追溯。

Purpose: 闭合 CONT-01/CONT-02，避免跨域泄露与“只给 include 不给 exclude 依据”的不可审计交付。
Output: Contract Envelope 数据结构、Bundle 构建中的 include/exclude rationale、WXT 正例回归测试。
</objective>

<execution_context>
@C:/Users/唐斌/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/唐斌/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-contracted-delivery/03-CONTEXT.md
@.planning/phases/03-contracted-delivery/03-RESEARCH.md
@internal/model/model.go
@internal/query/query.go
@tests/m1_minimal_test.go
@tests/m3_validation_closure_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在数据模型中定义 Contract Envelope 与决策字段</name>
  <files>internal/model/model.go</files>
  <action>在 ContextBundle 及其关联模型中新增契约对象（如 included/excluded decisions），每条决策必须包含 machine-readable 字段 `reason_code/source_rule/scope/decision_basis`，并包含 human-readable 短说明字段（例如 `human_note`）。字段命名可按现有模型风格调整，但必须保证 include 与 exclude 都可表达、可序列化、可被测试稳定断言。</action>
  <verify>go test ./... -run "TestContractBundleWXTPositive"</verify>
  <done>ContextBundle 能完整表达“包含什么/排除什么/依据是什么”，而非仅返回节点列表。</done>
</task>

<task type="auto">
  <name>Task 2: 在 BuildContextBundle 落地双条件门禁与 include/exclude 双向理由</name>
  <files>internal/query/query.go</files>
  <action>扩展 BuildContextBundle：required 判定必须同时满足“任务可完成性 + 规则可解释性”；对每个被包含与被排除节点生成 rationale；严格限制候选空间为目标主域 + 合法 attach-only capability，禁止跨域节点混入。若出现“完整性优先导致最小化放宽”，必须在 decision 中记录放宽原因并可追溯到规则来源（requirements/roadmap/core rules）。</action>
  <verify>go test ./... -run "TestContractBundleWXTPositive|TestRouteDomainCompetitionExcludesAttachOnlyCapability"</verify>
  <done>bundle 结果可同时回答“为何包含”与“为何排除”，且不会混入无关域内容。</done>
</task>

<task type="auto">
  <name>Task 3: 新增 WXT 正例契约测试锁定最小且完整行为</name>
  <files>tests/m3_contract_bundle_test.go</files>
  <action>新增契约正例测试文件，覆盖 WXT 样板：断言 required/main/must_include 仅含目标域必需节点；断言 included/excluded 决策都具备 machine-readable + human-readable 字段；断言 source_rule 可追溯；断言最小与完整冲突时有显式记录。测试不得依赖自然语言长文案，优先断言稳定字段。</action>
  <verify>go test ./... -run "TestContractBundleWXTPositive"</verify>
  <done>CONT-01/CONT-02 在自动化测试中具备稳定回归基线。</done>
</task>

</tasks>

<verification>
- 运行 `go test ./... -run "TestContractBundleWXTPositive"`，确认 WXT 正例满足最小且完整 + rationale 可追溯。
- 运行 `go test ./... -run "TestRouteDomainCompetitionExcludesAttachOnlyCapability"`，确认契约化 bundle 未破坏 attach-only 边界。
</verification>

<success_criteria>
- context bundle 新增 include/exclude rationale 且字段稳定可机检。
- 交付集合保持目标域边界，不出现跨域节点混入。
- 正例测试可重复通过，成为 Phase 3 后续检查输入。
</success_criteria>

<output>
After completion, create `.planning/phases/03-contracted-delivery/03-01-SUMMARY.md`
</output>
