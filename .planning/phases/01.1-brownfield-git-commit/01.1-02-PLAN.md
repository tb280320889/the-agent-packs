---
phase: 01.1-brownfield-git-commit
plan: 02
type: execute
wave: 2
depends_on:
  - 01.1-01
files_modified:
  - cmd/agent-pack-mcp/main.go
  - internal/activation/activation.go
  - internal/query/query.go
  - internal/registry/registry.go
  - internal/validator/runner.go
  - tests/m1_minimal_test.go
  - tests/m2_registry_test.go
  - tests/m3_validation_closure_test.go
  - .planning/phases/01.1-brownfield-git-commit/01.1-REFAC-COMMIT-PLAN.md
autonomous: true
requirements:
  - INDX-01
  - INDX-02
must_haves:
  truths:
    - "仓库核心代码与测试文件不再散落，位置与语义分层一致。"
    - "每一批迁移后都能运行基础验证，避免大爆炸式失败。"
    - "迁移提交按意图分批，审阅者可快速理解每批改动目的。"
  artifacts:
    - path: ".planning/phases/01.1-brownfield-git-commit/01.1-REFAC-COMMIT-PLAN.md"
      provides: "分批提交计划与每批验证证据要求"
      contains: "## Commit Slices"
    - path: "cmd/agent-pack-mcp/main.go"
      provides: "入口代码在新结构下可编译可运行"
      min_lines: 50
    - path: "tests/m1_minimal_test.go"
      provides: "迁移后回归验证"
      contains: "Test"
  key_links:
    - from: "01.1-MIGRATION-MAP.md"
      to: "实际 git mv/路径修复"
      via: "按批次执行迁移"
      pattern: "Batch"
    - from: "代码迁移批次"
      to: "go test ./..."
      via: "每批后回归"
      pattern: "go test ./..."
---

<objective>
执行仓库结构重排与路径修复：按 Plan 01 的迁移映射完成目录归位，保证每批可验证，并形成可直接执行的提交切片计划。

Purpose: 将“目标目录模型”落地到代码与测试资产，降低后续维护成本并避免根目录继续失控。
Output: 完成主要迁移与路径修复，产出提交切片计划。
</objective>

<execution_context>
@C:/Users/唐斌/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/唐斌/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-brownfield-git-commit/01.1-CONTEXT.md
@.planning/phases/01.1-brownfield-git-commit/01.1-RESEARCH.md
@.planning/phases/01.1-brownfield-git-commit/01.1-MIGRATION-MAP.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 执行结构迁移批次（仅移动，不改逻辑）</name>
  <files>
cmd/agent-pack-mcp/main.go
internal/activation/activation.go
internal/query/query.go
internal/registry/registry.go
internal/validator/runner.go
  </files>
  <action>严格按 01.1-MIGRATION-MAP.md 的 structure moves 批次执行迁移，优先使用 git mv 保留重命名语义。此任务禁止引入业务逻辑修改；若因编译报错需要调整，仅记录到下一批 path fixes 处理。执行后用 git diff --name-status -M 验证迁移状态。</action>
  <verify>git diff --name-status -M 输出主要为 R*/M* 且与迁移映射一致；无未映射的孤立文件。</verify>
  <done>结构重排完成且“移动动作”与“逻辑修复动作”边界清晰。</done>
</task>

<task type="auto">
  <name>Task 2: 修复路径引用并完成回归验证</name>
  <files>
cmd/agent-pack-mcp/main.go
tests/m1_minimal_test.go
tests/m2_registry_test.go
tests/m3_validation_closure_test.go
  </files>
  <action>在独立批次修复 import、文件路径常量、测试引用和脚本路径，确保迁移后可编译。完成修复后运行 go test ./...，并将失败修复限制在路径/结构兼容范围，不做新功能扩展。</action>
  <verify>go test ./... 全量通过；git diff 显示本批次以路径与引用修复为主。</verify>
  <done>迁移后系统行为与迁移前等价（在现有测试覆盖范围内），无新增功能改动。</done>
</task>

<task type="auto">
  <name>Task 3: 生成分批 commit 计划（按可审阅意图）</name>
  <files>.planning/phases/01.1-brownfield-git-commit/01.1-REFAC-COMMIT-PLAN.md</files>
  <action>依据当前改动生成提交切片计划，至少包含 4 类切片：refactor(structure)、refactor(paths)、docs(archive/mapping)、test/verify。每类切片写明包含文件、排除文件、提交消息模板、提交前验证命令。明确禁止一次性大提交。</action>
  <verify>文档包含 Commit Slices 表格，且每个切片都有 message 模板与 verify 步骤。</verify>
  <done>执行者可直接照计划逐批提交，不需再次拆分策略。</done>
</task>

</tasks>

<verification>
- 迁移动作与路径修复动作分离，避免混合提交。
- go test ./... 在路径修复后通过。
- commit 切片计划覆盖当前全部改动类别。
</verification>

<success_criteria>
- 仓库结构由“散乱”转为“按技术类型与领域可解释”。
- 每批改动都有对应验证命令与提交模板。
- 迁移没有引入超出 phase 边界的新功能。
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-brownfield-git-commit/01.1-02-SUMMARY.md`
</output>
