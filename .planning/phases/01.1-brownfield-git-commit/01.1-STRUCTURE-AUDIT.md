# Phase 01.1 结构审计

## Root Whitelist

以下路径保留在仓库根目录（必要入口与必要配置）：

| Path | Keep | Rationale |
|---|---|---|
| `.git/` | yes | Git 元数据目录，必须保留 |
| `.github/` | yes | CI/CD 与协作自动化入口 |
| `.gitignore` | yes | 仓库级忽略规则 |
| `.planning/` | yes | GSD 任务真相源与执行工件 |
| `AGENTS.md` | yes | 全局 agent 启动入口 |
| `blueprint/` | yes | Blueprint 资产与索引根目录 |
| `cmd/` | yes | 可执行入口层 |
| `docs/` | yes | AIDP 与归档文档层 |
| `fixtures/` | yes | 测试/示例输入输出 |
| `go.mod` | yes | Go module 定义 |
| `go.sum` | yes | 依赖锁定 |
| `internal/` | yes | 核心实现层 |
| `tests/` | yes | 测试层 |
| `tools/` | yes | 工具边界说明 |
| `workflow-packages/` | yes | workflow package 与注册表 |

## Current Issues

当前仓库结构整体已基本对齐“先技术类型、后领域细分”，本阶段重点是把规则与迁移口径文档化并冻结。

| Current Issue | Severity | Current Issue -> Target Location | Rationale |
|---|---|---|---|
| 缺少 Phase 01.1 的结构审计/目标树/迁移映射工件 | medium | `.planning/phases/01.1-brownfield-git-commit/*` -> 同路径新增标准工件 | 没有执行蓝图会导致后续重构过程不可审阅 |
| 缺少“先归档后删除”的标准归档区与清单 | medium | `docs/archive/2026-03-phase-01.1/*` | 删除门禁需要有可追踪清单 |
| 当前 phase 存在未纳入执行摘要的研究文档 | low | `.planning/phases/01.1-brownfield-git-commit/01.1-RESEARCH.md` -> 保留并纳入 commit 切片 | 需纳入 phase 产物闭环，避免悬空 |

## Scope Freeze

- 本阶段不做新功能开发，不改动 activation 顶层协议。
- 本阶段优先生成可执行映射与提交切片，不做无必要目录大搬迁。
- 删除动作保持 `human-approval` 门禁，默认不执行删除。
