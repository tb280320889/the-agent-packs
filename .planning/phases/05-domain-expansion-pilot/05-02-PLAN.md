---
phase: 05-domain-expansion-pilot
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - tests/m5_domain_expansion_regression_test.go
  - .planning/phases/05-domain-expansion-pilot/05-DOMAIN-ONBOARDING-CHECKLIST.md
autonomous: true
requirements:
  - DOMN-01
  - DOMN-02
must_haves:
  truths:
    - "新增第二主域后，WXT 样板 route/build_context_bundle/activate 三件套行为保持一致并可回归通过。"
    - "冲突语义场景下不会出现新主域抢占既有 WXT 主路径。"
    - "形成可复用的新主域准入清单，包含命名、路由、交付、验证与 runtime 证据链。"
  artifacts:
    - path: "tests/m5_domain_expansion_regression_test.go"
      provides: "DOMN-02 回归矩阵（WXT 不破坏 + 抗干扰）"
      contains: "TestM5DomainExpansionWXTNonRegression"
    - path: ".planning/phases/05-domain-expansion-pilot/05-DOMAIN-ONBOARDING-CHECKLIST.md"
      provides: "五段式新主域准入清单与证据模板"
      contains: "命名治理"
  key_links:
    - from: "tests/m5_domain_expansion_regression_test.go"
      to: "tests/m2_wxt_manifest_test.go"
      via: "WXT 既有主链路对照断言"
      pattern: "route_query|build_context_bundle|activate"
    - from: "tests/m5_domain_expansion_regression_test.go"
      to: "internal/activation/activation.go"
      via: "run_id/runtime_ledger 证据链断言"
      pattern: "CurrentValidationRunID|RuntimeLedger"
---

<objective>
完成 DOMN-02 抗回归验证，并沉淀可复用的新主域准入清单，确保后续扩域可复制。

Purpose: 证明第二主域试点不会破坏 WXT 样板契约，且输出可复用执行模板。
Output: WXT 非回归测试矩阵 + 五段式准入清单文档。
</objective>

<execution_context>
@C:/Users/唐斌/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/唐斌/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-domain-expansion-pilot/05-CONTEXT.md
@.planning/phases/05-domain-expansion-pilot/05-RESEARCH.md
@.planning/phases/05-domain-expansion-pilot/05-01-SUMMARY.md
@tests/m2_wxt_manifest_test.go
@tests/m1_minimal_test.go
@internal/activation/activation.go
@internal/query/query.go

<interfaces>
From internal/activation/activation.go:
- `Execute(db *sql.DB, requestPath string) (model.ActivationResult, error)`
- `ActivationResult.CurrentValidationRunID`
- `ActivationResult.RuntimeLedger []RuntimeLedgerEntry`

From internal/query/query.go:
- `RouteQuery(...) (model.RouteResult, error)`
- `BuildContextBundle(...) (model.ContextBundle, error)`

From tests/m2_wxt_manifest_test.go:
- 现有 WXT 样板断言基线（MainPack、ValidationPlan、Artifacts）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 增加 DOMN-02 回归矩阵，锁定 WXT 不破坏与冲突抗干扰</name>
  <files>tests/m5_domain_expansion_regression_test.go</files>
  <read_first>
    - tests/m2_wxt_manifest_test.go
    - tests/m1_minimal_test.go
    - tests/m5_domain_expansion_onboarding_test.go
    - internal/query/query.go
    - internal/activation/activation.go
    - .planning/phases/05-domain-expansion-pilot/05-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: 新域接入后，WXT route_query 主候选仍为 `wxt-manifest`。
    - Test 2: 新域接入后，WXT build_context_bundle 的 required_packs/contract decisions 与既有语义一致。
    - Test 3: 新域接入后，WXT activate 仍返回可追踪 run_id 与 runtime ledger，不发生契约断裂。
    - Test 4: 冲突词请求（同时包含 monorepo 与 extension 线索）不会抢占应落入 WXT 的请求。
  </behavior>
  <action>新建 `tests/m5_domain_expansion_regression_test.go`，采用表驱动覆盖 DOMN-02 场景。为 WXT 路由非回归新增 `TestM5DomainExpansionWXTNonRegression`：使用包含 extension 线索的 task 调 `RouteQuery`，断言 `Candidates[0].Pack == "wxt-manifest"`。为 bundle 非回归新增 `TestM5DomainExpansionWXTBundleContractNonRegression`：调用 `BuildContextBundle` 并断言 `RequiredPacks` 至少仍包含 `security-permissions` 与 `release-store-review`，且 include/exclude decisions 非空。为 activation 非回归新增 `TestM5DomainExpansionWXTActivationTraceNonRegression`：调用 `activation.Execute` 并断言 `CurrentValidationRunID` 非空、`RuntimeLedger` 非空且至少存在 `record_type=validation` 当前项。新增冲突抗干扰用例 `TestM5DomainExpansionConflictDoesNotStealWXT`，在 task 同时包含 monorepo/extension 词时，若 `target_domain=wxt` 则主候选仍为 WXT，防止新域抢占。</action>
  <acceptance_criteria>
    - `tests/m5_domain_expansion_regression_test.go` 包含 `TestM5DomainExpansionWXTNonRegression`。
    - `tests/m5_domain_expansion_regression_test.go` 包含 `TestM5DomainExpansionWXTBundleContractNonRegression`。
    - `tests/m5_domain_expansion_regression_test.go` 包含 `TestM5DomainExpansionWXTActivationTraceNonRegression`。
    - `tests/m5_domain_expansion_regression_test.go` 包含 `TestM5DomainExpansionConflictDoesNotStealWXT`。
    - 测试断言显式包含 `wxt-manifest`、`security-permissions`、`release-store-review`。
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestM5DomainExpansion(WXTNonRegression|WXTBundleContractNonRegression|WXTActivationTraceNonRegression|ConflictDoesNotStealWXT)" -count=1</automated>
  </verify>
  <done>DOMN-02 的 WXT 不破坏承诺具备自动化回归保护，新增主域无法静默抢占既有路径。</done>
</task>

<task type="auto">
  <name>Task 2: 产出五段式新主域准入清单（可复制到后续主域）</name>
  <files>.planning/phases/05-domain-expansion-pilot/05-DOMAIN-ONBOARDING-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/05-domain-expansion-pilot/05-CONTEXT.md
    - .planning/phases/05-domain-expansion-pilot/05-RESEARCH.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - docs/AIDP/core/06-业务规则与关键对象.md
  </read_first>
  <action>创建 `05-DOMAIN-ONBOARDING-CHECKLIST.md`，按锁定的“五段式”输出：1) 命名治理；2) 路由契约；3) 交付契约；4) 验证回归；5) runtime 证据链。每段必须给出可执行条目（命令、必备文件、通过标准、失败阻断标准）。文档必须显式写入四类 P0 阻断：`主域竞争越界`、`attach-only 破坏`、`WXT 回归失败`、`run_id/ledger 证据链断裂`；并写入 warn 项必须包含 `deadline` 与 `runtime ledger` 留痕要求。新增“最小证据集合”小节，固定为 `command + artifact output + run_id + runtime-ledger refs`。</action>
  <acceptance_criteria>
    - `05-DOMAIN-ONBOARDING-CHECKLIST.md` 包含 5 个一级段落：`命名治理`、`路由契约`、`交付契约`、`验证回归`、`runtime 证据链`。
    - 文档包含 4 条 P0 阻断并逐条具名。
    - 文档包含 `最小证据集合` 且包含 `run_id` 与 `runtime-ledger` 字样。
    - 文档包含至少 3 条可执行命令（`go test` 或等效命令）。
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestM5DomainExpansion.*" -count=1</automated>
  </verify>
  <done>形成可复用的新主域准入模板，后续扩域可直接复用而无需重新定义验收口径。</done>
</task>

</tasks>

<verification>
- `go test ./... -run "TestM5DomainExpansion.*" -count=1` 通过。
- `go test ./... -run "TestM2Activation.*" -count=1` 通过（确认 WXT 样板未被破坏）。
</verification>

<success_criteria>
- WXT 样板主链路在第二主域接入后仍稳定通过。
- 冲突场景下不出现新主域抢占 WXT 的回归。
- 新主域准入清单形成并可直接复用到后续领域线。
</success_criteria>

<output>
After completion, create `.planning/phases/05-domain-expansion-pilot/05-02-SUMMARY.md`
</output>
