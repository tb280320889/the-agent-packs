---
phase: 05-domain-expansion-pilot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - blueprint/L0/monorepo/overview.md
  - blueprint/L1/monorepo/oss-governance.md
  - workflow-packages/monorepo-oss-governance/package.yaml
  - workflow-packages/registry.json
  - internal/query/query.go
  - tests/m5_domain_expansion_onboarding_test.go
autonomous: true
requirements:
  - DOMN-01
must_haves:
  truths:
    - "第二主域 monorepo-oss-governance 可按现有 registry/routing 契约被路由为主包。"
    - "新增主域不改变 attach-only 语义：capability 仍只在主包确认后附挂。"
    - "可通过 feature switch 快速回退到仅 WXT 主域路径。"
  artifacts:
    - path: "workflow-packages/registry.json"
      provides: "第二主域 package 身份与 canonical 映射真相源"
      contains: "\"name\": \"monorepo-oss-governance\""
    - path: "internal/query/query.go"
      provides: "新域准入开关与候选空间控制"
      contains: "DOMAIN_MONOREPO_ENABLED"
    - path: "tests/m5_domain_expansion_onboarding_test.go"
      provides: "新域准入 + 开关回退自动化回归"
      contains: "TestM5DomainExpansionOnboardMonorepoRouteAndActivation"
  key_links:
    - from: "workflow-packages/registry.json"
      to: "blueprint/L1/monorepo/oss-governance.md"
      via: "canonical_blueprint_node"
      pattern: "L1.monorepo.oss-governance"
    - from: "internal/query/query.go"
      to: "tests/m5_domain_expansion_onboarding_test.go"
      via: "RouteQuery/BuildContextBundle 行为断言"
      pattern: "DOMAIN_MONOREPO_ENABLED|RouteQuery"
---

<objective>
在不改动既有治理骨架的前提下，接入第二主域 monorepo-oss-governance 并打通单子域闭环。

Purpose: 满足 DOMN-01（第二主域可按现有契约准入与激活），为 DOMN-02 回归验证提供可执行基线。
Output: 新域 blueprint + package + registry + 路由开关 + 准入回归测试。
</objective>

<execution_context>
@C:/Users/唐斌/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/唐斌/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-domain-expansion-pilot/05-CONTEXT.md
@.planning/phases/05-domain-expansion-pilot/05-RESEARCH.md
@workflow-packages/registry.json
@internal/query/query.go
@tests/m2_registry_test.go
@tests/m2_wxt_manifest_test.go

<interfaces>
From internal/query/query.go:
- `RouteQuery(...) (model.RouteResult, error)`
- `BuildContextBundle(...) (model.ContextBundle, error)`

From internal/registry/registry.go:
- `Validate(reg Registry, packagesRoot string) error`
- workflow 包命名规则：`name = domain-subdomain` 且 category=workflow 不得 attach-only

From workflow-packages/registry.json:
- 现有 workflow: `wxt-manifest`
- 现有 capability: `security-permissions`, `release-store-review`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 落地第二主域资产与注册表准入（Blueprint + Package + Registry）</name>
  <files>blueprint/L0/monorepo/overview.md, blueprint/L1/monorepo/oss-governance.md, workflow-packages/monorepo-oss-governance/package.yaml, workflow-packages/registry.json</files>
  <read_first>
    - blueprint/L0/wxt/overview.md
    - blueprint/L1/wxt/manifest.md
    - workflow-packages/wxt-manifest/package.yaml
    - workflow-packages/registry.json
    - internal/registry/registry.go
    - .planning/phases/05-domain-expansion-pilot/05-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: registry 校验可通过，且新包 canonical 映射到 L1.monorepo.oss-governance。
    - Test 2: 新主域为 workflow category + direct activation，不会被误设为 attach-only。
    - Test 3: 现有 capability 线保持原样（security/release 仍 attach-only，不做替换迁移）。
  </behavior>
  <action>新增 `blueprint/L0/monorepo/overview.md` 与 `blueprint/L1/monorepo/oss-governance.md`，frontmatter 使用与现有 L0/L1 一致字段：`id/level/domain/subdomain/node_kind/visibility_scope/activation_mode/aliases/triggers/anti_triggers/required_with/may_include/children/entry_conditions/stop_conditions`。其中 L0 节点 `id=L0.monorepo`、`domain=monorepo`、`visibility_scope=global`、`activation_mode=direct`；L1 节点 `id=L1.monorepo.oss-governance`、`subdomain=oss-governance`、`visibility_scope=domain-scoped`、`activation_mode=direct`。新增 `workflow-packages/monorepo-oss-governance/package.yaml`，字段对齐现有 workflow 包，`name=monorepo-oss-governance`、`domain=monorepo`、`subdomain=oss-governance`、`depends_on=[]`、`validators` 至少包含 `validator-core-output`、`artifacts` 至少包含 `monorepo-oss-governance-review.md`。更新 `workflow-packages/registry.json` 注册该包，`category=workflow`、`canonical_blueprint_node=L1.monorepo.oss-governance`、`visibility_scope=domain-scoped`、`activation_mode=direct`，且不改动既有 WXT 与 capability 条目。</action>
  <acceptance_criteria>
    - `workflow-packages/registry.json` 包含 `"name": "monorepo-oss-governance"`。
    - `workflow-packages/registry.json` 对应条目包含 `"canonical_blueprint_node": "L1.monorepo.oss-governance"`。
    - `workflow-packages/registry.json` 对应条目包含 `"category": "workflow"` 与 `"activation_mode": "direct"`。
    - `blueprint/L0/monorepo/overview.md` 包含 `id: L0.monorepo`。
    - `blueprint/L1/monorepo/oss-governance.md` 包含 `id: L1.monorepo.oss-governance`。
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestM2RegistryLoadsAndValidates" -count=1</automated>
  </verify>
  <done>第二主域的节点、包定义、注册表映射全部落位，且 registry 校验通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现新域 feature switch 与 DOMN-01 准入回归</name>
  <files>internal/query/query.go, tests/m5_domain_expansion_onboarding_test.go</files>
  <read_first>
    - internal/query/query.go
    - tests/m2_wxt_manifest_test.go
    - tests/m2_registry_test.go
    - workflow-packages/registry.json
    - .planning/phases/05-domain-expansion-pilot/05-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `DOMAIN_MONOREPO_ENABLED=true` 时，monorepo 相关 task 可路由到 `monorepo-oss-governance`。
    - Test 2: 关闭开关时，同 task 不再进入 monorepo 主候选，形成可回退行为。
    - Test 3: attach-only 边界不变：capability 不进入 primary candidate。
  </behavior>
  <action>在 `internal/query/query.go` 增加新域开关函数，读取环境变量 `DOMAIN_MONOREPO_ENABLED`（默认 `true`），仅在开关开启时允许 `domain=monorepo` 节点进入 L0/L1 candidate-space。保持 candidate-space-first 与 attach-only 两阶段逻辑不变，不允许 capability 抢主包。新增 `tests/m5_domain_expansion_onboarding_test.go`：覆盖 `RouteQuery` 与 `activation.Execute` 的新域准入正例（主包=monorepo-oss-governance）以及开关关闭回退例（不可选中新域）。测试中显式断言 `RouteResult.Candidates[0].Pack`、`DecisionBasis`、`ActivationResult.MainPack` 与 `RouteStatus`。</action>
  <acceptance_criteria>
    - `internal/query/query.go` 出现 `DOMAIN_MONOREPO_ENABLED`。
    - `tests/m5_domain_expansion_onboarding_test.go` 包含 `TestM5DomainExpansionOnboardMonorepoRouteAndActivation`。
    - `tests/m5_domain_expansion_onboarding_test.go` 包含 `TestM5DomainExpansionFeatureSwitchRollback`。
    - 测试断言含 `monorepo-oss-governance` 与 `ROUTE_NO_PRIMARY_CANDIDATE`（回退场景）。
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestM5DomainExpansion(OnboardMonorepoRouteAndActivation|FeatureSwitchRollback)" -count=1</automated>
  </verify>
  <done>第二主域可受控启停并完成 route->activate 闭环，满足可快速回退要求。</done>
</task>

</tasks>

<verification>
- `go test ./... -run "TestM2RegistryLoadsAndValidates" -count=1`
- `go test ./... -run "TestM5DomainExpansion(OnboardMonorepoRouteAndActivation|FeatureSwitchRollback)" -count=1`
</verification>

<success_criteria>
- 新主域 monorepo-oss-governance 在 registry/routing 契约下可准入。
- feature switch 能一键回退到仅 WXT 稳定路径。
- attach-only 能力线未被破坏。
</success_criteria>

<output>
After completion, create `.planning/phases/05-domain-expansion-pilot/05-01-SUMMARY.md`
</output>
