---
phase: 01-foundation-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - tests/m4_regression_compilation_test.go
  - fixtures/blueprint/frontmatter-multi-line.md
  - fixtures/registry/package-with-unknown-field.yaml
autonomous: true
requirements:
  - PARS-01
  - PARS-02
must_haves:
  truths:
    - "解析与索引改动不会破坏现有最小闭环测试"
    - "新增解析规则有回归覆盖，避免再次滑回手写解析"
  artifacts:
    - path: "tests/m4_regression_compilation_test.go"
      provides: "解析与索引回归覆盖"
      min_lines: 80
    - path: "fixtures/blueprint/frontmatter-multi-line.md"
      provides: "多行/引号/列表 frontmatter 样例"
      min_lines: 20
    - path: "fixtures/registry/package-with-unknown-field.yaml"
      provides: "未知字段 package.yaml 样例"
      min_lines: 10
  key_links:
    - from: "tests/m4_regression_compilation_test.go"
      to: "compiler.Compile"
      via: "回归测试"
      pattern: "Compile\("
---

<objective>
补齐解析与索引硬化的回归覆盖与固定样例，确保新行为可持续验证。

Purpose: 防止后续修改再次引入手写解析或静默失败。
Output: 回归测试 + 固定样例 fixture。
</objective>

<execution_context>
@C:/Users/唐斌/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/唐斌/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-hardening/01-CONTEXT.md
@.planning/phases/01-foundation-hardening/01-RESEARCH.md
@tests/m1_minimal_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加解析回归测试与固定样例</name>
  <files>tests/m4_regression_compilation_test.go, fixtures/blueprint/frontmatter-multi-line.md, fixtures/registry/package-with-unknown-field.yaml</files>
  <action>
    - 新增 frontmatter 多行/引号/列表的 fixture（用于验证 yaml.v3 解析）。
    - 新增 package.yaml 含未知字段 fixture（用于验证 KnownFields 报错）。
    - 测试中调用 compiler.Compile 或 registry 解析入口，断言：
      1) 正确样例可解析成功；
      2) 未知字段样例显式失败并包含字段名。
  </action>
  <verify>go test ./... -run TestM4RegressionParsing</verify>
  <done>新增 fixture 被测试引用且断言通过</done>
</task>

<task type="auto">
  <name>Task 2: 运行全量测试确保闭环不回退</name>
  <files>tests/m4_regression_compilation_test.go</files>
  <action>
    - 运行 `go test ./...`，确保现有最小闭环测试仍通过。
    - 若因新错误结构/返回值改动导致旧测试失败，更新旧测试断言以适配结构化错误输出（不得回退为字符串错误）。
  </action>
  <verify>go test ./...</verify>
  <done>全量测试通过且结构化错误仍被覆盖</done>
</task>

</tasks>

<verification>
- `go test ./...` 通过
</verification>

<success_criteria>
- 新解析规则与事务性索引重建均有回归覆盖
- 现有闭环测试不回退
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-hardening/01-03-SUMMARY.md`
</output>
