---
phase: 01-foundation-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/compiler/compiler.go
  - tests/m4_index_rebuild_test.go
  - tests/m4_compile_errors_test.go
autonomous: true
requirements:
  - INDX-01
must_haves:
  truths:
    - "当报告写入失败时，上一版 blueprint.db 仍可被查询使用"
    - "索引替换仅在报告成功后发生，失败路径不会留下半成品索引"
    - "失败路径会返回结构化 index/report 阶段错误，便于 CLI/MCP 与测试消费"
  artifacts:
    - path: "internal/compiler/compiler.go"
      provides: "Compile 流程的事务化索引替换与失败回滚时序"
      contains: "Compile(rootDir, dbPath, reportDir)"
    - path: "tests/m4_index_rebuild_test.go"
      provides: "报告失败时旧索引保留、成功时索引替换的回归验证"
      contains: "report write failure"
    - path: "tests/m4_compile_errors_test.go"
      provides: "失败路径结构化错误输出断言"
      contains: "PhaseReport"
  key_links:
    - from: "internal/compiler/compiler.go"
      to: "tests/m4_index_rebuild_test.go"
      via: "Compile 失败/成功路径测试"
      pattern: "Compile\(.*\)|report_write|index_swap"
    - from: "internal/compiler/compiler.go"
      to: "blueprint/index/blueprint.db"
      via: "tmp + bak 原子替换"
      pattern: "\.tmp|\.bak|os\.Rename"
gap_closure: true
---

<objective>
修复 Phase 01 验证报告中的唯一缺口：索引在报告写入失败时仍可能被提前替换，导致“可恢复性”不成立。

Purpose: 让 INDX-01 的“失败不破坏旧索引”从代码时序到测试证据都可重复验证。
Output: 事务化替换时序修复 + 回归测试补强 + 可复验命令结果。
</objective>

<execution_context>
@C:/Users/唐斌/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/唐斌/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-hardening/01-VERIFICATION.md
@.planning/phases/01-foundation-hardening/01-02-SUMMARY.md
@internal/compiler/compiler.go
@tests/m4_index_rebuild_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 Compile 中索引替换时序，消除报告失败前写入</name>
  <files>internal/compiler/compiler.go</files>
  <action>移除/重构 `Compile` 中任何在 `writeReports` 前直接写入目标 `dbPath` 的路径（例如重复调用 `writeIndex(dbPath, ...)`），确保索引写入流程严格为：写入 tmp -> 写报告 -> 备份旧 DB -> 原子替换 -> 清理备份。保留并完善失败回滚：若替换失败且已创建备份，恢复旧 DB；所有失败路径返回结构化 `CompilerError`（phase/path/code/message），避免 silent failure。这样可以直接闭合 VERIFICATION.md 中“report 失败导致旧索引不可保证”的缺口。</action>
  <verify>go test ./... -run TestM4IndexRebuild && go test ./... -run TestM4CompileErrors</verify>
  <done>当报告写入失败时，Compile 返回 report 阶段结构化错误且旧索引未被覆盖；当报告成功时，索引才发生替换。</done>
</task>

<task type="auto">
  <name>Task 2: 补强索引可恢复性回归测试并执行全量回归</name>
  <files>tests/m4_index_rebuild_test.go, tests/m4_compile_errors_test.go</files>
  <action>在 `m4_index_rebuild_test` 中新增/强化用例：先准备可查询的旧索引，再构造报告写入失败场景，执行 Compile 后断言旧索引内容仍可查询（而非仅断言报错）；同时增加成功路径断言确保新索引确实替换。必要时在 `m4_compile_errors_test` 补充 `report_write` / `index_swap` 错误码断言，确保失败原因可解释。不要只做“err != nil”浅断言，必须覆盖旧索引可用性这一用户可观察真值。</action>
  <verify>go test ./... -run TestM4IndexRebuild && go test ./... -run TestM4CompileErrors && go test ./...</verify>
  <done>测试稳定复现并证明：报告失败不破坏旧索引，成功路径替换索引；全量测试通过无回归。</done>
</task>

</tasks>

<verification>
- 运行 `go test ./... -run TestM4IndexRebuild`，确认失败场景下旧索引可查询。
- 运行 `go test ./... -run TestM4CompileErrors`，确认 `report_write`/`index_swap` 等结构化错误可见。
- 运行 `go test ./...`，确认 Phase 01 的解析与索引硬化能力无回退。
</verification>

<success_criteria>
- `Compile` 不再在报告成功前写入最终 `dbPath`。
- 报告写入失败后，旧索引文件和查询结果保持可用。
- 相关回归测试覆盖失败与成功双路径并持续通过。
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-hardening/01-04-SUMMARY.md`
</output>
