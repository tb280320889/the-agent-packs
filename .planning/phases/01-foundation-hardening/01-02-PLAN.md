---
phase: 01-foundation-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - internal/compiler/compiler.go
  - internal/compiler/compiler_errors.go
  - internal/model/model.go
  - cmd/agent-pack-mcp/main.go
  - tests/m4_compile_errors_test.go
  - tests/m4_index_rebuild_test.go
  - tests/m4_cli_compile_output_test.go
autonomous: true
requirements:
  - INDX-01
  - INDX-02
---

<objective>
实现事务化索引重建与结构化编译错误输出，确保失败可回滚且可测试。
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-hardening/01-CONTEXT.md
@.planning/phases/01-foundation-hardening/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义结构化编译错误并贯穿编译阶段</name>
  <files>internal/compiler/compiler_errors.go, internal/model/model.go, internal/compiler/compiler.go</files>
  <action>
    - 增加 parse/index/report 阶段结构化错误字段
    - 编译返回 CompileResult(errors)
  </action>
  <verify>go test ./... -run TestM4CompileErrors</verify>
  <done>结构化错误可被测试覆盖</done>
</task>

<task type="auto">
  <name>Task 2: 实现索引重建的临时 DB + 原子替换回滚</name>
  <files>internal/compiler/compiler.go</files>
  <action>
    - 临时 DB 写入完成后再替换目标 DB
    - 失败保留旧索引
  </action>
  <verify>go test ./... -run TestM4IndexRebuildTransactional</verify>
  <done>索引重建失败不破坏旧索引</done>
</task>

<task type="auto">
  <name>Task 3: 调整 CLI/MCP 输出以返回结构化错误</name>
  <files>cmd/agent-pack-mcp/main.go</files>
  <action>
    - CLI/MCP 输出包含 CompileResult.errors
  </action>
  <verify>go test ./... -run TestM4CLICompileOutput</verify>
  <done>CLI/MCP 输出结构化错误结果</done>
</task>

</tasks>

<verification>
- `go test ./...` 通过
</verification>

<success_criteria>
- INDX-01/INDX-02 可被结构化错误覆盖与回滚机制保证
</success_criteria>
