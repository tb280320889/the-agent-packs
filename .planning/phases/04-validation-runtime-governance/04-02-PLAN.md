---
phase: 04-validation-runtime-governance
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - internal/activation/activation.go
  - internal/query/query.go
  - internal/model/model.go
  - tests/m4_validation_governance_test.go
autonomous: true
requirements:
  - VALD-01
must_haves:
  truths:
    - "validation plan 由 registry 映射稳定生成，包含 core + domain validator，不依赖手工硬编码清单。"
    - "关键里程碑、规则变更、validator 清单变化会自动触发 validation；手动补跑可执行且有标记。"
    - "failed(error) 只阻断当前 plan，warned(warn) 可继续但必须输出留痕建议。"
  artifacts:
    - path: "internal/activation/activation.go"
      provides: "混合触发判定 + registry 计划生成 + plan 级阻断语义"
      contains: "buildValidationPlan"
    - path: "internal/query/query.go"
      provides: "validator 清单签名摘要（用于触发规则）"
      contains: "RecommendedValidators"
    - path: "tests/m4_validation_governance_test.go"
      provides: "VALD-01 触发与计划生成回归测试"
      contains: "TestM4ValidationPlanGeneratedFromRegistry"
  key_links:
    - from: "internal/activation/activation.go"
      to: "workflow-packages/registry.json"
      via: "bundle.RecommendedValidators + required_packs 解析"
      pattern: "validator-core-output|validator-domain-wxt-manifest"
    - from: "internal/activation/activation.go"
      to: "internal/model/model.go"
      via: "TriggerKind/TriggerReason + status 分流"
      pattern: "milestone_auto|rule_change_auto|validator_manifest_change_auto|manual_rerun"
---

<objective>
把 validation 计划生成与触发机制制度化：从 registry 一致生成 validator 清单，并按混合触发策略执行 plan 级阻断。

Purpose: 满足 VALD-01，消除“靠会话记忆决定何时验证、验证哪些规则”的隐式路径。
Output: registry 驱动的 validation plan 生成逻辑、混合触发判定、阻断分流回归测试。
</objective>

<execution_context>
@C:/Users/唐斌/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/唐斌/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-runtime-governance/04-CONTEXT.md
@workflow-packages/registry.json
@internal/activation/activation.go
@internal/query/query.go
@tests/m3_validation_closure_test.go
@tests/m4_validation_governance_test.go

<interfaces>
From internal/activation/activation.go:
- buildValidationPlan(req *requestShape, mainPack string, artifacts []model.Artifact, bundleValidators []string) model.ValidationPlan
- deriveStatus(..., plan model.ValidationPlan, results []model.ValidatorResult) string

From internal/model/model.go:
- type ValidationPlan struct { Validators []ValidatorPlan; SeverityPolicy map[string]string; PlanReason string }
- type ValidatorPlan struct { Name string; Scope string; Reason string }

From workflow-packages/registry.json:
- packages[].recommended_validators
- packages[].required_packs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 用 registry 统一生成 core+domain validation 计划</name>
  <files>internal/activation/activation.go, internal/query/query.go, internal/model/model.go</files>
  <read_first>
    - internal/activation/activation.go
    - internal/query/query.go
    - internal/model/model.go
    - workflow-packages/registry.json
    - .planning/phases/04-validation-runtime-governance/04-CONTEXT.md
  </read_first>
  <action>改造 `buildValidationPlan`：不再依赖仅来自 main bundle 的单一 validator 列表，改为聚合 `main_pack + required_packs` 在 registry 中的 `recommended_validators`，去重后生成计划。强制保证 `validator-core-output` 存在且排在首位；其余 validator 按字典序稳定排序，确保同输入计划稳定。每个 validator 的 `scope` 规则固定为：`validator-core-output -> artifact`，`validator-domain-*` 或 `validator-contract-* -> domain`；`reason` 必须写入具体触发语句（如 `Registry declared validator for wxt-manifest`）。</action>
  <acceptance_criteria>
    - `internal/activation/activation.go` 中 `buildValidationPlan` 包含 `required_packs` 聚合逻辑。
    - 生成计划时 `validator-core-output` 总是存在且在 validators 列表首位。
    - validator 排序逻辑包含稳定排序（`sort.Strings` 或等价实现）。
    - `ValidationPlan.PlanReason` 含有 `registry-defined plan` 文本。
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestM3GoldenCompleted" -count=1</automated>
  </verify>
  <done>VALD-01 的“core + domain validator 从 registry 一致生成”具备可执行实现与稳定输出。</done>
</task>

<task type="auto">
  <name>Task 2: 落地混合触发判定与 plan 级阻断分流</name>
  <files>internal/activation/activation.go, internal/model/model.go</files>
  <read_first>
    - internal/activation/activation.go
    - internal/model/model.go
    - .planning/phases/04-validation-runtime-governance/04-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>在 activation 增加触发类型枚举并透传到 validation 输出：`milestone_auto`（默认关键里程碑自动触发）、`rule_change_auto`（规则码或 source_rule 变化）、`validator_manifest_change_auto`（validator 清单签名变化）、`manual_rerun`（显式补跑）。为请求增加可选参数 `validation_manual_rerun`（bool），为 true 时强制 `TriggerKind=manual_rerun`。状态模型必须分层并写死：`ValidationMachineView.Status` 仅允许三态 `passed|warned|failed`；`ActivationResult.Status` 继续沿用流程态 `completed|partial|failed`。映射固定为：存在 error finding 或任一 validator status=failed -> `ValidationMachineView.Status=failed` 且 `ActivationResult.Status=failed`；存在 warn finding 且无 failed -> `ValidationMachineView.Status=warned` 且 `ActivationResult.Status=partial`，并在 `HumanView.NextActions` 添加至少一条留痕动作（例如 `记录 warned 处理意见并链接 run_id`）；无 warning/error -> `ValidationMachineView.Status=passed` 且 `ActivationResult.Status=completed`。阻断只作用于当前 activation 结果，不新增跨 phase 阻断逻辑。</action>
  <acceptance_criteria>
    - 代码中出现 4 种 TriggerKind：`milestone_auto`、`rule_change_auto`、`validator_manifest_change_auto`、`manual_rerun`。
    - `validation_manual_rerun=true` 时输出 `TriggerKind=manual_rerun`。
    - `ValidationMachineView.Status` 仅出现并仅使用 `passed|warned|failed` 三态。
    - `ActivationResult.Status` 映射固定为 `completed|partial|failed`，且与 `ValidationMachineView.Status` 映射关系在代码中可见。
    - warned 路径下 `HumanView.NextActions` 至少包含 1 条留痕动作字符串。
    - 代码未引入任何跨 phase 阻断状态字段（如 `cross_phase_blocked`）。
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestM3GoldenCompleted" -count=1</automated>
  </verify>
  <done>触发与分流规则符合锁定决策：自动+手动并存、warned 可继续但留痕、failed 阻断当前 plan。</done>
</task>

<task type="auto">
  <name>Task 3: 补齐 M4 回归测试（计划生成 + 触发 + 分流）</name>
  <files>tests/m4_validation_governance_test.go</files>
  <read_first>
    - tests/m4_validation_governance_test.go
    - tests/m3_validation_closure_test.go
    - internal/activation/activation.go
    - workflow-packages/registry.json
  </read_first>
  <action>在 `tests/m4_validation_governance_test.go` 新增用例：`TestM4ValidationPlanGeneratedFromRegistry`（断言 validators 来自 registry 且含 core+domain，且执行后 `ValidatorResults` 至少包含 `validator-core-output` 与 1 个 domain validator 的结果条目，并且这些结果与当前 `RunID` 同批次输出关联）、`TestM4ValidationTriggerKindsAndPlanScopedBlocking`（覆盖自动触发、手动补跑、warned->partial、failed->failed，并断言 `ValidationMachineView.Status` 三态与 `ActivationResult.Status` 映射关系）。两条测试均需断言输出字段包含 `TriggerKind`、`MachineView.Status`、`HumanView.NextActions`。测试必须使用现有 `compileMainIndex/openDB/writeRequestFile` 模式，避免引入新测试框架。</action>
  <acceptance_criteria>
    - `tests/m4_validation_governance_test.go` 包含 `TestM4ValidationPlanGeneratedFromRegistry`。
    - `tests/m4_validation_governance_test.go` 包含 `TestM4ValidationTriggerKindsAndPlanScopedBlocking`。
    - 两个测试都断言 `TriggerKind` 与 `MachineView.Status`。
    - `TestM4ValidationPlanGeneratedFromRegistry` 断言 `ValidatorResults` 至少包含 core + domain 执行结果条目。
    - `TestM4ValidationPlanGeneratedFromRegistry` 断言结果条目与当前 `RunID` 同批次关联（通过 envelope/run 输出字段关联）。
    - `go test` 执行不依赖外部服务或人工输入。
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestM4Validation(PlanGeneratedFromRegistry|TriggerKindsAndPlanScopedBlocking)" -count=1</automated>
  </verify>
  <done>VALD-01 的核心行为具备可重复自动化回归保护。</done>
</task>

</tasks>

<verification>
- `go test ./... -run "TestM4Validation(PlanGeneratedFromRegistry|TriggerKindsAndPlanScopedBlocking)" -count=1` 通过。
- `go test ./... -run "TestM3GoldenCompleted" -count=1` 继续通过。
</verification>

<success_criteria>
- validator 计划由 registry 驱动生成，包含 core + domain。
- 混合触发（自动+手动补跑）在输出中可见可验。
- failed/warned/passed 分流符合 plan 级阻断策略。
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-runtime-governance/04-02-SUMMARY.md`
</output>
