---
phase: 04-validation-runtime-governance
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - docs/AIDP/runtime/01-默认假设账本.md
  - docs/AIDP/runtime/02-决策日志.md
  - docs/AIDP/runtime/03-变更摘要.md
  - docs/AIDP/runtime/04-phase-context.md
  - docs/AIDP/runtime/06-验证记录.md
  - internal/model/model.go
  - internal/activation/activation.go
  - tests/m4_validation_governance_test.go
autonomous: true
requirements:
  - GOVR-01
must_haves:
  truths:
    - "每次关键变化都能在 runtime 账本中看到 assumption/decision/change/validation 记录。"
    - "runtime 账本记录包含 trace_id/run_id、时间戳、来源引用、状态与版本标记。"
    - "允许延后补记但有 deadline 与风险升级字段，且历史版本不被覆盖。"
  artifacts:
    - path: "internal/model/model.go"
      provides: "runtime ledger 记录结构与字段约束"
      contains: "type RuntimeLedgerEntry"
    - path: "internal/activation/activation.go"
      provides: "关键事件即时回写 + 收尾批量回写入口"
      contains: "LedgerWriteMode"
    - path: "docs/AIDP/runtime/06-验证记录.md"
      provides: "Phase 4 验证闭环记录"
      contains: "V-08"
  key_links:
    - from: "internal/activation/activation.go"
      to: "docs/AIDP/runtime/01-默认假设账本.md"
      via: "默认延后窗口与假设来源回写"
      pattern: "assumption|DeferredDeadline|24h"
    - from: "internal/activation/activation.go"
      to: "docs/AIDP/runtime/06-验证记录.md"
      via: "validation run_id 与验证记录条目关联"
      pattern: "run_id"
    - from: "internal/activation/activation.go"
      to: "docs/AIDP/runtime/02-决策日志.md"
      via: "关键决策事件回写"
      pattern: "decision"
    - from: "internal/activation/activation.go"
      to: "docs/AIDP/runtime/03-变更摘要.md"
      via: "规则/实现变更摘要回写"
      pattern: "change|version"
gap_closure: false
---

<objective>
建立 Phase 4 的 runtime 治理回写制度：关键事件即时回写、其余收尾批量回写，并保持版本追加与审计可追踪。

Purpose: 满足 GOVR-01，让运行态账本从“可选文档”升级为“验证闭环的一部分”。
Output: runtime ledger 结构、写入策略、AIDP runtime 工件更新规则与自动化回归测试。
</objective>

<execution_context>
@C:/Users/唐斌/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/唐斌/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-runtime-governance/04-CONTEXT.md
@docs/AIDP/runtime/01-默认假设账本.md
@docs/AIDP/runtime/02-决策日志.md
@docs/AIDP/runtime/03-变更摘要.md
@docs/AIDP/runtime/04-phase-context.md
@docs/AIDP/runtime/06-验证记录.md
@internal/model/model.go
@internal/activation/activation.go
@tests/m4_validation_governance_test.go

<interfaces>
From internal/model/model.go (from 04-01/04-02 expected output):
- ValidationEnvelope.RunID / PhaseID / PlanID / IsCurrentEffective

From docs/AIDP/runtime/*:
- runtime 文档现有承载 assumption / decision / change / validation 四类记录，需保持追加式更新。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 runtime ledger 记录结构与版本追加规则</name>
  <files>internal/model/model.go, internal/activation/activation.go</files>
  <read_first>
    - internal/model/model.go
    - internal/activation/activation.go
    - .planning/phases/04-validation-runtime-governance/04-CONTEXT.md
    - docs/AIDP/runtime/01-默认假设账本.md
    - docs/AIDP/runtime/02-决策日志.md
    - docs/AIDP/runtime/03-变更摘要.md
    - docs/AIDP/runtime/06-验证记录.md
  </read_first>
  <action>在 `internal/model/model.go` 新增 `RuntimeLedgerEntry`：字段至少包含 `TraceID string`、`RunID string`、`RecordType string`（限定 assumption/decision/change/validation）、`Timestamp string`（RFC3339 UTC）、`SourceRefs []string`、`Status string`、`Version int`、`IsCurrent bool`、`DeferredReason string`、`DeferredDeadline string`、`RiskEscalated bool`。在 `internal/activation/activation.go` 增加写入模式枚举 `LedgerWriteMode`，仅允许 `immediate`（关键事件）与 `batch_finalize`（计划收尾）。关键事件判定固定：规则变更、failed 结论、阻断决策 -> immediate；其余 -> batch_finalize。实现版本追加策略：同一 `TraceID+RecordType` 追加新版本，旧版本 `IsCurrent=false`，新版本 `IsCurrent=true`，不得覆盖历史条目。</action>
  <acceptance_criteria>
    - `internal/model/model.go` 包含 `type RuntimeLedgerEntry struct`。
    - `RecordType` 允许值在代码中显式列出 `assumption|decision|change|validation`。
    - `internal/activation/activation.go` 包含 `LedgerWriteMode` 且仅有 `immediate` 与 `batch_finalize`。
    - 代码包含 `IsCurrent=false` 的旧版本降级逻辑。
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestM3GoldenCompleted" -count=1</automated>
  </verify>
  <done>runtime 账本结构与版本追加策略被代码级固化，可用于后续统一回写。</done>
</task>

<task type="auto">
  <name>Task 2: 落地延后补记窗口与风险升级逻辑</name>
  <files>internal/activation/activation.go, tests/m4_validation_governance_test.go</files>
  <read_first>
    - internal/activation/activation.go
    - tests/m4_validation_governance_test.go
    - .planning/phases/04-validation-runtime-governance/04-CONTEXT.md
  </read_first>
  <action>在 activation 的 ledger 构建逻辑中实现延后补记字段：当写入模式为 `batch_finalize` 且当前未完成收尾时，写入 `DeferredReason` 与 `DeferredDeadline`（默认窗口 24h，可配置但默认值必须写死为 `24h`）。若当前时间超过 `DeferredDeadline` 仍未补记，则置 `RiskEscalated=true` 并在 human summary 增加 `runtime-ledger-overdue` 提示。保留 `RunID` 与 `TraceID` 原值，禁止覆盖历史条目。为该逻辑新增测试：覆盖“窗口内未升级”与“超窗升级”两条路径。</action>
  <acceptance_criteria>
    - 代码中出现默认延后窗口 `24h`。
    - 代码包含 `RiskEscalated=true` 的超窗分支。
    - 测试覆盖窗口内与超窗两种路径。
    - 延后路径下仍保留 `RunID` 与 `TraceID` 字段。
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestM4RuntimeLedgerDeferredWindowAndEscalation" -count=1</automated>
  </verify>
  <done>延后补记不再是无约束口头动作，具备 deadline 与风险升级机制。</done>
</task>

<task type="auto">
  <name>Task 3: 更新 AIDP runtime 工件并登记 Phase 4 验证闭环记录</name>
  <files>docs/AIDP/runtime/01-默认假设账本.md, docs/AIDP/runtime/02-决策日志.md, docs/AIDP/runtime/03-变更摘要.md, docs/AIDP/runtime/04-phase-context.md, docs/AIDP/runtime/06-验证记录.md</files>
  <read_first>
    - docs/AIDP/runtime/01-默认假设账本.md
    - docs/AIDP/runtime/02-决策日志.md
    - docs/AIDP/runtime/03-变更摘要.md
    - docs/AIDP/runtime/04-phase-context.md
    - docs/AIDP/runtime/06-验证记录.md
    - .planning/phases/04-validation-runtime-governance/04-CONTEXT.md
    - .planning/ROADMAP.md
  </read_first>
  <action>按追加式方式更新 AIDP runtime 文档：
1) 在 `01-默认假设账本.md` 追加新假设（若使用了 24h 默认延后窗口）并标记 `assumed`；
2) 在 `02-决策日志.md` 追加 Phase 4 决策：混合触发、plan 级阻断、run_id 证据链、append-only ledger；
3) 在 `03-变更摘要.md` 追加 Phase 4 变更原因/变化/不变项/后续维护；
4) 在 `04-phase-context.md` 追加当前阶段更新与风险；
5) 在 `06-验证记录.md` 新增 `V-08` 条目，验证方式明确写入 `go test ./... -run "TestM4.*"` 与结果状态。禁止删除或覆盖既有条目，只允许追加新节。</action>
  <acceptance_criteria>
    - 五个 runtime 文档均新增 "2026-03" 的 Phase 4 追加内容。
    - `docs/AIDP/runtime/06-验证记录.md` 包含 `V-08`。
    - `V-08` 条目包含命令 `go test ./... -run "TestM4.*"`。
    - 所有更新为追加，不删除既有编号条目。
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestM4(RuntimeLedgerVersionAppendOnly|RuntimeLedgerDeferredWindowAndEscalation)" -count=1</automated>
  </verify>
  <done>GOVR-01 对应的 runtime 账本回写规则与运行态文档留痕全部落地。</done>
</task>

</tasks>

<verification>
- `go test ./... -run "TestM4(RuntimeLedgerVersionAppendOnly|RuntimeLedgerDeferredWindowAndEscalation)" -count=1` 通过。
- `go test ./... -run "TestM4Validation.*" -count=1` 通过（确保 run_id/trace 与 ledger 逻辑兼容）。
</verification>

<success_criteria>
- runtime 关键事件可即时回写，非关键事件可批量回写但有 deadline 与风险升级。
- 账本记录具备 trace_id/run_id/record_type/timestamp/source_refs/status/version/is_current。
- AIDP runtime 五类工件完成 Phase 4 追加回写，且保持 append-only 历史。
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-runtime-governance/04-03-SUMMARY.md`
</output>
