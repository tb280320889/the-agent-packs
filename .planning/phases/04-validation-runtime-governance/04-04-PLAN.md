---
phase: 04-validation-runtime-governance
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - internal/activation/activation.go
  - internal/model/model.go
  - tests/m4_validation_governance_test.go
autonomous: true
requirements:
  - GOVR-01
gap_closure: true
must_haves:
  truths:
    - "关键事件进入 runtime ledger 时，不再只有 validation 单一类型，而是可按规则产出 assumption/decision/change/validation。"
    - "record_type 的产出规则在代码中可读、可测、可回归，避免仅靠常量定义。"
    - "四类 record_type 都可在 activation 治理路径被触发，并保持 append-only 版本追踪。"
  artifacts:
    - path: "internal/activation/activation.go"
      provides: "key change -> runtime ledger record_type 映射与多类型写入逻辑"
      contains: "BuildRuntimeLedgerEntries"
    - path: "internal/model/model.go"
      provides: "runtime ledger record_type 枚举与映射使用约束"
      contains: "RuntimeLedgerRecordTypeAssumption"
    - path: "tests/m4_validation_governance_test.go"
      provides: "四类 record_type 触发与版本链回归测试"
      contains: "TestM4RuntimeLedgerRecordTypesFromKeyChanges"
  key_links:
    - from: "internal/activation/activation.go"
      to: "internal/model/model.go"
      via: "RuntimeLedgerRecordType 常量与映射函数"
      pattern: "RuntimeLedgerRecordType(Assumption|Decision|Change|Validation)"
    - from: "internal/activation/activation.go"
      to: "tests/m4_validation_governance_test.go"
      via: "key change 到 record_type 的断言矩阵"
      pattern: "record_type|BuildRuntimeLedgerEntries"
---

<objective>
修复 Phase 4 验证缺口：把 runtime ledger 从“仅 validation 写入”升级为“按关键事件写入四类 record_type”。

Purpose: 闭合 04-VERIFICATION.md 中 GOVR-01 阻断缺口，确保关键变更回写制度真正落地到执行路径。
Output: record_type 映射规则实现、多类型 ledger 写入、四类类型可触发的自动化回归测试。
</objective>

<execution_context>
@C:/Users/唐斌/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/唐斌/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-runtime-governance/04-CONTEXT.md
@.planning/phases/04-validation-runtime-governance/04-VERIFICATION.md
@.planning/phases/04-validation-runtime-governance/04-03-SUMMARY.md
@internal/model/model.go
@internal/activation/activation.go
@tests/m4_validation_governance_test.go

<interfaces>
From internal/model/model.go:
- RuntimeLedgerRecordTypeAssumption / Decision / Change / Validation（四类常量已存在，当前缺口是执行路径未消费）

From internal/activation/activation.go:
- BuildRuntimeLedgerEntries(...)（当前固定写入 RuntimeLedgerRecordTypeValidation，需要改为规则映射）

From .planning/phases/04-validation-runtime-governance/04-VERIFICATION.md:
- Gap reason: "BuildRuntimeLedgerEntries 内 entry.RecordType 固定为 RuntimeLedgerRecordTypeValidation"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 activation 路径实现 key change → record_type 多类型映射</name>
  <files>internal/activation/activation.go, internal/model/model.go</files>
  <read_first>
    - internal/activation/activation.go
    - internal/model/model.go
    - .planning/phases/04-validation-runtime-governance/04-VERIFICATION.md
    - .planning/phases/04-validation-runtime-governance/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: 任意 validation run 都至少写入 1 条 `record_type=validation`。
    - Test 2: `TriggerKind=rule_change_auto` 或 `validator_manifest_change_auto` 时，额外写入 `record_type=change`。
    - Test 3: `ValidationMachineView.Status=failed` 或 `TriggerKind=manual_rerun` 时，额外写入 `record_type=decision`。
    - Test 4: `LedgerWriteMode=batch_finalize` 且存在延后补记信息（`DeferredReason`/`DeferredDeadline`）时，额外写入 `record_type=assumption`。
  </behavior>
  <action>修改 `BuildRuntimeLedgerEntries`，禁止固定 `entry.RecordType = RuntimeLedgerRecordTypeValidation` 的单写入模式。按以下明确规则组装 `recordTypes` 列表并逐条生成 entry：1) 永远包含 `validation`；2) 当 `TriggerKind` 为 `rule_change_auto` 或 `validator_manifest_change_auto`，添加 `change`；3) 当 machine 状态为 `failed` 或 `TriggerKind=manual_rerun`，添加 `decision`；4) 当写入模式为 `batch_finalize` 且 `DeferredReason` 非空或 `DeferredDeadline` 已设置，添加 `assumption`。去重后按固定顺序写入（`assumption, decision, change, validation`），确保同输入产生稳定输出。维持 append-only：同一 `TraceID+RecordType` 追加新版本，旧版本 `IsCurrent=false`，新版本 `IsCurrent=true`。</action>
  <acceptance_criteria>
    - `internal/activation/activation.go` 不再出现固定赋值 `RecordType: RuntimeLedgerRecordTypeValidation` 的单 entry 路径。
    - 代码中出现对 `rule_change_auto` 与 `validator_manifest_change_auto` 的 `change` 映射。
    - 代码中出现对 `manual_rerun` 或 `failed` 的 `decision` 映射。
    - 代码中出现 `batch_finalize` + deferred 条件的 `assumption` 映射。
    - 版本处理逻辑仍按 `TraceID+RecordType` 更新 `Version` 与 `IsCurrent`。
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestM4RuntimeLedger(RecordTypesFromKeyChanges|VersionAppendOnly)" -count=1</automated>
  </verify>
  <done>activation 治理路径可按关键事件产出四类 runtime ledger 记录，不再受限于 validation 单类型。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新增回归矩阵，锁定四类 record_type 可触发与版本链稳定</name>
  <files>tests/m4_validation_governance_test.go</files>
  <read_first>
    - tests/m4_validation_governance_test.go
    - internal/activation/activation.go
    - internal/model/model.go
    - .planning/phases/04-validation-runtime-governance/04-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: 关键变更触发矩阵可覆盖四类 record_type。
    - Test 2: 同一 TraceID 下各 record_type 版本独立追加，旧版本自动降级为 `IsCurrent=false`。
  </behavior>
  <action>在 `tests/m4_validation_governance_test.go` 新增并实现：`TestM4RuntimeLedgerRecordTypesFromKeyChanges`（表驱动至少 4 组输入：普通里程碑、rule_change_auto、manual_rerun+failed、batch_finalize+deferred；断言输出 record_type 集合分别包含 validation/change/decision/assumption）与 `TestM4RuntimeLedgerRecordTypeVersionAppendOnly`（同一 TraceID 连续写入两轮，断言每个被触发的 record_type 都满足 version 递增、旧版本 `IsCurrent=false`、最新版本 `IsCurrent=true`）。测试必须直接断言 `entry.RecordType` 的具体字符串值，禁止仅检查常量存在。</action>
  <acceptance_criteria>
    - `tests/m4_validation_governance_test.go` 包含 `func TestM4RuntimeLedgerRecordTypesFromKeyChanges`。
    - `tests/m4_validation_governance_test.go` 包含 `func TestM4RuntimeLedgerRecordTypeVersionAppendOnly`。
    - 测试断言中显式出现四种类型：`assumption`、`decision`、`change`、`validation`。
    - 测试断言中显式检查 `Version` 与 `IsCurrent` 的版本追加行为。
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestM4RuntimeLedger(RecordTypesFromKeyChanges|RecordTypeVersionAppendOnly|VersionAppendOnly|DeferredWindowAndEscalation)" -count=1</automated>
  </verify>
  <done>GOVR-01 缺口具备稳定回归保护，后续改动无法再退化为单一 validation 记录。</done>
</task>

</tasks>

<verification>
- `go test ./... -run "TestM4RuntimeLedger(RecordTypesFromKeyChanges|RecordTypeVersionAppendOnly|VersionAppendOnly|DeferredWindowAndEscalation)" -count=1` 通过。
- `go test ./... -run "TestM4Validation.*" -count=1` 通过（确认 validation trace 与 runtime ledger 兼容）。
</verification>

<success_criteria>
- 04-VERIFICATION.md 的 GOVR-01 缺口（单一 validation record_type）被代码级修复。
- activation 执行路径可触发 assumption/decision/change/validation 四类 ledger 记录。
- key change 到 record_type 的映射在测试中有明确断言并可重复执行。
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-runtime-governance/04-04-SUMMARY.md`
</output>
