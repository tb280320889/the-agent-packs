---
phase: 04-validation-runtime-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/model/model.go
  - internal/validator/types.go
  - internal/activation/activation.go
  - tests/m4_validation_governance_test.go
autonomous: true
requirements:
  - VALD-02
must_haves:
  truths:
    - "每次 validation 结果都带有 run_id，且绑定 phase/plan 维度。"
    - "validation 输出可追溯到 artifacts 与 handoff 引用，不再只有状态文本。"
    - "同一次 activation 输出同时提供 machine-readable 结构化视图与 human summary。"
  artifacts:
    - path: "internal/model/model.go"
      provides: "Validation 追踪结构（run_id、evidence refs、双视图字段）"
      contains: "type ValidationEvidenceRef"
    - path: "internal/activation/activation.go"
      provides: "生成并填充 run_id + evidence 链路"
      contains: "CurrentValidationRunID"
    - path: "tests/m4_validation_governance_test.go"
      provides: "validation trace contract 回归测试"
      contains: "TestM4ValidationTraceLinksArtifactsAndHandoff"
  key_links:
    - from: "internal/activation/activation.go"
      to: "internal/model/model.go"
      via: "ValidationEnvelope 新字段填充"
      pattern: "RunID|EvidenceRefs|MachineView|HumanSummary"
    - from: "internal/activation/activation.go"
      to: "ActivationResult.Handoff"
      via: "evidence 引用 handoff"
      pattern: "handoff"
---

<objective>
为 Phase 4 建立 validation 追踪契约：把 run_id、证据链与双视图输出固化到 activation 结果中。

Purpose: 满足“validation result 与 artifacts/handoff 可追踪关联”的硬约束，避免再次退化为会话口头约定。
Output: 扩展后的 validation 数据结构、activation 填充逻辑、可重复执行的追踪回归测试。
</objective>

<execution_context>
@C:/Users/唐斌/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/唐斌/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-runtime-governance/04-CONTEXT.md
@internal/model/model.go
@internal/activation/activation.go
@internal/validator/types.go
@tests/m3_validation_closure_test.go

<interfaces>
From internal/model/model.go:
- type ValidationEnvelope struct { ValidationPlan ValidationPlan; ValidatorResults []ValidatorResult }
- type ActivationResult struct { ValidationResults []ValidationEnvelope; Handoff map[string]any }

From internal/validator/types.go:
- type ExecutionInput struct { Task string; MainPack string; ContractBundle *model.ContextBundle; Artifacts []model.Artifact; Handoff map[string]any }

From internal/activation/activation.go:
- Execute(db *sql.DB, requestPath string) (model.ActivationResult, error)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 validation trace 契约结构（run_id + 证据引用 + 双视图）</name>
  <files>internal/model/model.go, internal/validator/types.go</files>
  <read_first>
    - internal/model/model.go
    - internal/validator/types.go
    - .planning/phases/04-validation-runtime-governance/04-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>在 `internal/model/model.go` 新增并导出结构：`ValidationEvidenceRef{RefID string, RefType string, RefPath string, StrongLink bool}`、`ValidationMachineView{Status string, ErrorCodes []string, RuleCodes []string, Trigger string, RepairSuggestions []string}`、`ValidationHumanView{Summary string, NextActions []string}`。扩展 `ValidationEnvelope` 增加字段：`RunID string`、`PhaseID string`、`PlanID string`、`TriggerKind string`、`TriggerReason string`、`IsCurrentEffective bool`、`InputDigest string`、`EvidenceRefs []ValidationEvidenceRef`、`MachineView ValidationMachineView`、`HumanView ValidationHumanView`。扩展 `ActivationResult` 增加 `ValidationRunHistory []ValidationEnvelope` 与 `CurrentValidationRunID string`。在 `internal/validator/types.go` 的 `ExecutionInput` 增加 `PhaseID string`、`PlanID string`、`TriggerKind string`、`TriggerReason string`，默认用于后续链路透传。</action>
  <acceptance_criteria>
    - `internal/model/model.go` 包含 `type ValidationEvidenceRef struct`。
    - `internal/model/model.go` 的 `ValidationEnvelope` 包含字段名 `RunID`、`EvidenceRefs`、`MachineView`、`HumanView`。
    - `internal/model/model.go` 的 `ActivationResult` 包含字段名 `ValidationRunHistory`、`CurrentValidationRunID`。
    - `internal/validator/types.go` 的 `ExecutionInput` 包含 `PhaseID`、`PlanID`、`TriggerKind`、`TriggerReason`。
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestM3GoldenCompleted" -count=1</automated>
  </verify>
  <done>validation trace 数据契约可被编译并可由 activation 层直接填充。</done>
</task>

<task type="auto">
  <name>Task 2: 在 activation 输出中填充 run_id 证据链与双视图</name>
  <files>internal/activation/activation.go</files>
  <read_first>
    - internal/activation/activation.go
    - internal/model/model.go
    - internal/validator/types.go
    - .planning/phases/04-validation-runtime-governance/04-CONTEXT.md
  </read_first>
  <action>在 `Execute` 中生成 `run_id`（格式：`{request_id}:validation:{unix_ts_or_uuid}`），并为请求增加可选字段 `phase_id`、`plan_id`、`validation_trigger_kind`、`validation_trigger_reason`：当字段存在时透传到 validation 输出，不存在时分别回退为 `phase_id="04"`、`plan_id="unknown"`、`validation_trigger_kind="milestone_auto"`、`validation_trigger_reason="plan_milestone_validation"`。为当前 validation 结果填充 `EvidenceRefs`：必须从本次 `ExecutionInput.Artifacts` 动态生成至少 1 条 `artifact:{artifact_path}` 引用（禁止硬编码具体文件名），并在存在 handoff 时加入 `handoff:{request_id}`，始终加入 `runtime-ledger:{run_id}` 强引用。`MachineView` 必须填充 `status/error_codes/rule_codes/trigger/repair_suggestions`；`HumanView.Summary` 以一句中文摘要说明结论并给出 `NextActions`。将历史数组写入 `ActivationResult.ValidationRunHistory`，并把当前 run 标记为 `IsCurrentEffective=true` 与 `CurrentValidationRunID=run_id`。</action>
  <acceptance_criteria>
    - `internal/activation/activation.go` 出现 `CurrentValidationRunID` 赋值。
    - `internal/activation/activation.go` 出现 `IsCurrentEffective: true`。
    - `internal/activation/activation.go` 中 evidence 引用包含 `runtime-ledger:` 前缀。
    - `internal/activation/activation.go` 中 machine view 填充包含 `ErrorCodes` 与 `RuleCodes`。
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestM3GoldenCompleted" -count=1</automated>
  </verify>
  <done>单次 activation 输出具备完整 validation 证据链与双视图，不再仅有状态值。</done>
</task>

<task type="auto">
  <name>Task 3: 新增 M4 回归测试锁定 trace 字段与引用关系</name>
  <files>tests/m4_validation_governance_test.go</files>
  <read_first>
    - tests/m3_validation_closure_test.go
    - internal/activation/activation.go
    - internal/model/model.go
    - .planning/phases/04-validation-runtime-governance/04-CONTEXT.md
  </read_first>
  <action>新增测试文件 `tests/m4_validation_governance_test.go`，实现至少 2 个用例：`TestM4ValidationTraceContractStructs`（反射或编译期字段存在性断言）与 `TestM4ValidationTraceLinksArtifactsAndHandoff`（执行 activation 后断言 `CurrentValidationRunID != ""`、`ValidationRunHistory` 非空、`EvidenceRefs` 同时包含 artifact/handoff/runtime-ledger 三类引用、`MachineView.Status` 与 envelope 状态一致、`HumanView.Summary` 非空）。测试命名保持 `TestM4...` 前缀，沿用 `compileMainIndex/openDB/writeRequestFile` 测试工具函数。</action>
  <acceptance_criteria>
    - 存在 `tests/m4_validation_governance_test.go`。
    - 文件包含 `func TestM4ValidationTraceContractStructs`。
    - 文件包含 `func TestM4ValidationTraceLinksArtifactsAndHandoff`。
    - 两个测试都断言 `CurrentValidationRunID` 与 `EvidenceRefs`。
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run "TestM4ValidationTrace.*" -count=1</automated>
  </verify>
  <done>VALD-02 的最小追踪链具备可重复自动化验证口径。</done>
</task>

</tasks>

<verification>
- `go test ./... -run "TestM4ValidationTrace.*" -count=1` 必须通过。
- `go test ./... -run "TestM3GoldenCompleted" -count=1` 必须继续通过（确保不破坏 Phase 3 基线）。
</verification>

<success_criteria>
- 每次 validation 结果均有 run_id 且关联 phase/plan。
- validation 输出同时包含 machine-readable 与 human summary 双视图。
- artifacts/handoff/runtime ledger 三类证据引用可从结果直接追溯。
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-runtime-governance/04-01-SUMMARY.md`
</output>
