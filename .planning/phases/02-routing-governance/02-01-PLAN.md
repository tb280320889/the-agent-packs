---
phase: 02-routing-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/query/query.go
  - tests/m1_minimal_test.go
  - tests/m3_validation_closure_test.go
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
must_haves:
  truths:
    - "route_query 先完成候选空间过滤，再执行评分与排序。"
    - "attach-only capability 不参与 primary 主竞争，只能在主包确认后附挂。"
    - "同一输入在冲突场景下返回稳定可复现的主候选。"
  artifacts:
    - path: "internal/query/query.go"
      provides: "candidate-space-first + 两阶段 primary/attach 决策"
      contains: "RouteQuery"
    - path: "tests/m1_minimal_test.go"
      provides: "路由治理主回归（过滤、attach-only、稳定决策）"
      contains: "TestRouteDomainCompetitionExcludesAttachOnlyCapability"
    - path: "tests/m3_validation_closure_test.go"
      provides: "跨阶段负例验证（错误 target_pack 不被 hint 覆写）"
      contains: "TestM3NegativeWrongTargetPackNotOverriddenByHint"
  key_links:
    - from: "internal/query/query.go"
      to: "tests/m1_minimal_test.go"
      via: "L0/L1 候选过滤与 attach-only 断言"
      pattern: "RouteQuery|attach-only|candidate_space"
    - from: "internal/query/query.go"
      to: "tests/m3_validation_closure_test.go"
      via: "target_pack 优先级与稳定 tie-break"
      pattern: "target_pack|tie-break|stable"
---

<objective>
把 route 核心重构为严格治理内核：先过滤合法候选空间，再评分排序，并将 capability 固化为 attach-only 第二阶段行为。

Purpose: 闭合 ROUT-01/ROUT-02，防止非法候选污染主决策并保证 attach-only 边界不漂移。
Output: 两阶段 RouteQuery 实现 + 冲突稳定判定 + 回归测试矩阵。
</objective>

<execution_context>
@C:/Users/唐斌/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/唐斌/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-routing-governance/02-CONTEXT.md
@.planning/phases/02-routing-governance/02-RESEARCH.md
@internal/query/query.go
@tests/m1_minimal_test.go
@tests/m3_validation_closure_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 RouteQuery 为 candidate-space-first 管线</name>
  <files>internal/query/query.go</files>
  <action>将候选处理拆为“合法候选空间过滤 -> 评分 -> 稳定排序”三段，禁止在评分前混入不合法节点。L0 仅允许 domain-root/domain-orchestrator 主竞争；L1 workflow 主竞争仅允许当前主域内 direct/global 非 attach-only 节点。capability 节点仅进入 attach 池，不进入主候选池，满足主域优先与 attach-only 边界。</action>
  <verify>go test ./... -run "TestRouteL0OnlyReturnsDomainRootCandidates|TestRouteDomainCompetitionExcludesAttachOnlyCapability|TestRouteRespectsTargetDomain"</verify>
  <done>RouteQuery 在代码结构上明确先过滤后评分，且 attach-only 节点不会出现在 primary candidates。</done>
</task>

<task type="auto">
  <name>Task 2: 固化稳定冲突决策链并保留可复现依据</name>
  <files>internal/query/query.go</files>
  <action>为同分冲突实现稳定 tie-break 规则：canonical 命中优先 > 明确主域匹配 > 规则优先级 > 名称字典序；禁止依赖 map/遍历顺序或随机行为。将本次决策依据写入路由结果可消费字段（例如 decision_basis），确保调用方可复验为何命中该主包。</action>
  <verify>go test ./... -run "TestRouteTargetPackHasHighestPriority|TestM3NegativeWrongTargetPackNotOverriddenByHint"</verify>
  <done>同输入多次 route 返回相同主候选，并能在结果中看到稳定决策依据。</done>
</task>

<task type="auto">
  <name>Task 3: 扩展治理回归用例锁死候选污染与附挂边界</name>
  <files>tests/m1_minimal_test.go, tests/m3_validation_closure_test.go</files>
  <action>补充/更新测试覆盖：候选空间先过滤再评分、attach-only 不进主竞争、target_pack 优先级不被 hints 覆写、must_include 去重稳定。测试断言必须以 machine-readable 字段为主，不只检查文案片段。</action>
  <verify>go test ./... -run "TestRoute.*|TestM3NegativeWrongTargetPackNotOverriddenByHint"</verify>
  <done>出现回退（候选污染、attach-only 漏入、优先级漂移）时测试可稳定失败。</done>
</task>

</tasks>

<verification>
- 运行 `go test ./... -run "TestRoute.*"`，确认候选空间过滤与 attach-only 行为稳定。
- 运行 `go test ./... -run "TestM3NegativeWrongTargetPackNotOverriddenByHint"`，确认目标包优先级不被上下文 hints 反向覆盖。
</verification>

<success_criteria>
- RouteQuery 的主决策路径不再出现“先评分再过滤”的实现。
- capability attach-only 只出现在附挂列表，不出现在 primary candidates。
- 冲突场景结果可复现且可解释。
</success_criteria>

<output>
After completion, create `.planning/phases/02-routing-governance/02-01-SUMMARY.md`
</output>
