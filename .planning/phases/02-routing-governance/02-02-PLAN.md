---
phase: 02-routing-governance
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - internal/model/model.go
  - internal/query/query.go
  - internal/activation/activation.go
  - cmd/agent-pack-mcp/main.go
  - tests/m1_minimal_test.go
  - tests/m3_validation_closure_test.go
autonomous: true
requirements:
  - ROUT-03
  - ROUT-04
must_haves:
  truths:
    - "route/activate 返回主域与主包命中依据，且 capability 附挂/拒绝原因可读可解析。"
    - "canonical 映射缺失或不可路由时返回显式 failed/partial，不再静默 fallback。"
    - "默认输出极简必要字段，并提供下一步建议与 docs_ref 预留位。"
  artifacts:
    - path: "internal/model/model.go"
      provides: "可解释路由结果与错误语义结构（最小字段 + 可扩展字段）"
      contains: "type RouteResult"
    - path: "internal/query/query.go"
      provides: "ROUTE_CANONICAL_MISSING 显式失败语义与附挂原因编码"
      contains: "RouteQuery"
    - path: "internal/activation/activation.go"
      provides: "消费路由解释结果并输出一致 status/summary"
      contains: "Execute"
    - path: "cmd/agent-pack-mcp/main.go"
      provides: "MCP route_query/activate 输出兼容新解释字段"
      contains: "tools/call"
    - path: "tests/m1_minimal_test.go"
      provides: "canonical 缺失与解释字段回归"
      contains: "TestRouteTargetPackHasHighestPriority"
    - path: "tests/m3_validation_closure_test.go"
      provides: "activation 输出语义回归"
      contains: "TestM3GoldenCompleted"
  key_links:
    - from: "internal/query/query.go"
      to: "internal/model/model.go"
      via: "RouteResult 结构化解释字段"
      pattern: "error_code|decision_basis|docs_ref"
    - from: "internal/query/query.go"
      to: "internal/activation/activation.go"
      via: "route status 影响 activation status/summary"
      pattern: "RouteQuery|status|next_action"
    - from: "cmd/agent-pack-mcp/main.go"
      to: "tests/m1_minimal_test.go"
      via: "CLI/MCP 响应字段一致性"
      pattern: "route_query|activate"
---

<objective>
把 route/activate 输出升级为“默认极简、可扩展明细”的可解释契约，并移除 canonical 缺失时的静默 fallback。

Purpose: 闭合 ROUT-03/ROUT-04，让调用方 agent 在不暴露过量内部细节的前提下，稳定获取可执行解释与错误语义。
Output: 结构化解释字段 + canonical missing 显式失败 + 端到端回归测试。
</objective>

<execution_context>
@C:/Users/唐斌/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/唐斌/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-routing-governance/02-CONTEXT.md
@.planning/phases/02-routing-governance/02-RESEARCH.md
@.planning/phases/02-routing-governance/02-01-PLAN.md
@internal/model/model.go
@internal/query/query.go
@internal/activation/activation.go
@cmd/agent-pack-mcp/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义可解释路由结果契约并实现默认极简输出</name>
  <files>internal/model/model.go, internal/query/query.go</files>
  <action>在 RouteResult 中补充稳定 machine-readable 字段：status、error_code、message、next_action、decision_basis、decision_trace_id、docs_ref，以及主决策与 capability 附挂结果的原因码/规则码。默认仅返回极简必要信息；保留 detail 扩展位但不默认填充冗长链路。字段命名可由 Claude 自主设计，但必须满足 CONTEXT 锁定决策的“双层模型 + 可操作失败建议 + docs_ref 预留”。</action>
  <verify>go test ./... -run "TestRouteDomainCompetitionExcludesAttachOnlyCapability|TestM3NegativeWrongTargetPackNotOverriddenByHint"</verify>
  <done>route 结果可直接被调用方程序解析出主决策依据、capability 附挂/拒绝原因与下一步建议。</done>
</task>

<task type="auto">
  <name>Task 2: 删除 canonical fallback 并固化 ROUTE_CANONICAL_MISSING 显式失败语义</name>
  <files>internal/query/query.go, internal/activation/activation.go, cmd/agent-pack-mcp/main.go</files>
  <action>移除 target_pack 分支的 `registry fallback` 候选返回。当 registry canonical 缺失、节点不存在、级别不可路由或无法稳定判定时，返回显式失败/partial（优先 hard fail）并携带稳定错误码 `ROUTE_CANONICAL_MISSING`、短 message、固定 next_action（检查 registry canonical 映射）和 docs_ref 占位。同步 activation 与 MCP 输出，确保 route 错误语义不被吞掉、不回退硬编码包。</action>
  <verify>go test ./... -run "TestRouteTargetPackHasHighestPriority|TestActivationPartialWhenDomainKnownButNoCandidate"</verify>
  <done>不存在任何 canonical 缺失时仍返回 candidate 的路径；调用方能稳定拿到错误码与短建议。</done>
</task>

<task type="auto">
  <name>Task 3: 增加解释契约与失败语义回归矩阵</name>
  <files>tests/m1_minimal_test.go, tests/m3_validation_closure_test.go</files>
  <action>补充并调整测试：断言主域/主包/capability 的原因码与规则标识存在；断言 canonical 缺失或不可路由场景返回 `ROUTE_CANONICAL_MISSING` 且不含 fallback candidate；断言 failure/partial 响应包含固定 next_action 与 docs_ref 字段（可为空）。测试应覆盖 CLI/MCP 可见字段，不依赖自由文本措辞。</action>
  <verify>go test ./... -run "TestRoute.*|TestActivation.*|TestM3.*" && go test ./...</verify>
  <done>ROUT-03/ROUT-04 行为在正例与负例下均可复现验证，且回退路径被测试锁死。</done>
</task>

</tasks>

<verification>
- 运行 `go test ./... -run "TestRoute.*"`，确认 route 解释字段与 canonical 失败语义稳定。
- 运行 `go test ./... -run "TestActivation.*|TestM3.*"`，确认 activation 对 route 解释/错误语义消费正确。
- 运行 `go test ./...`，确认全量回归通过。
</verification>

<success_criteria>
- route/activate 输出包含主决策与 capability 附挂解释字段，且默认保持极简。
- canonical 缺失时返回显式错误码与短建议，不再出现 registry fallback。
- 路由解释与失败语义能被测试稳定断言，不依赖文案变化。
</success_criteria>

<output>
After completion, create `.planning/phases/02-routing-governance/02-02-SUMMARY.md`
</output>
